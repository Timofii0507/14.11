#ifndef FUNCTIONS_H
#define FUNCTIONS_H
#include <iostream>
#include <Windows.h>
using namespace std;

int findMin(int a, int b);
int findMax(int a, int b);
int square(int x);
int power(int x, int n);
bool isEven(int x);
bool isOdd(int x);

#endif 
